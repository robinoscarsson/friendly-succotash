# friendly-succotash

A modern, responsive static site built with Bootstrap.

## Development

To run the site locally, start a simple HTTP server:

```bash
python3 -m http.server 8000
```

Then open your browser to:

```
http://localhost:8000/
```